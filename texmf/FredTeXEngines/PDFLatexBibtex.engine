#!/bin/tcsh

set path= ($path /usr/texbin /usr/local/bin)
set filename = $1
set bibname = ${filename:r}.aux
pdflatex $filename
echo $bibname
bibtex --min-crossrefs=5 $bibname
pdflatex $filename
pdflatex $filename
