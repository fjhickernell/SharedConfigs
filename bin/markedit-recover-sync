#!/bin/zsh

# Compatibility entry point for repairing only the managed MarkEdit links.

set -euo pipefail

script_dir=${0:A:h}
exec "$script_dir/sharedconfigs-audit" --repair --all --links-only "$@" --group markedit
